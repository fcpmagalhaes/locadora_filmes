<div class="container">
	<div class="row">
		<div class="col-lg-12">
			<h1 class="page-header"><%=pluralize(@genres.size, "Gênero", "Gêneros")%>
				<small class="novo_filme"><%= link_to 'Novo Gênero', new_genre_path %></small>
			</h1>
		</div>
	</div>

	<ul>
		<% @genres.each do |genre| %>
			<li><%= link_to genre.name, genre %></li>
		<%end%>
	</ul>
</div>